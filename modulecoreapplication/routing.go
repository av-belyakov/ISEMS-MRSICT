package modulecoreapplication

import (
	"fmt"
)

//Routing обеспечивает маршрутизацию всех данных циркулирующих внутри приложения
func Routing(clim *ChannelsListInteractingModules) {
	fmt.Println("func 'Routing', START...")

	for {

	}
}
